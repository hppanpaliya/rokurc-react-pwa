# Runtime Environment Variables (injected at container runtime)
# These are injected into the React app via /config.js endpoint

# Backend URL for API proxy (leave empty for same-container deployment)
REACT_APP_BACKEND_URL=

# Environment (development/production)
REACT_APP_ENVIRONMENT=production

# Server port (for the Express server)
PORT=12312